# Integration Tests

Integration tests that require PostgreSQL, Redis, or other local infrastructure belong here and should be marked with `@pytest.mark.integration`.

M1.1 unit and contract tests do not require external services.
